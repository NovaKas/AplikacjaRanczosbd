@model IEnumerable<AplikacjaRanczo.Models.Rasa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nazwa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.skrot)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nazwa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.skrot)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RasaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RasaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RasaID })
        </td>
    </tr>
}

</table>
