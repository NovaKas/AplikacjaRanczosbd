@model IEnumerable<AplikacjaRanczo.Models.Miejscowosc>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Wojewodztwo.nazwa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nazwa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Wojewodztwo.nazwa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nazwa)
        </td>
        <td>
            @Html.ActionLink("Edytuj", "Edit", new { id=item.MiejscowoscID }) |
            @Html.ActionLink("Szczegóły", "Details", new { id=item.MiejscowoscID }) |
            @Html.ActionLink("Usuń", "Delete", new { id=item.MiejscowoscID })
        </td>
    </tr>
}

</table>
